name: papermind

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: papermind-db
    environment:
      POSTGRES_USER: papermind
      POSTGRES_PASSWORD: papermind
      POSTGRES_DB: papermind
    ports:
      # 5432 is standard; override via .env if your machine has another postgres there.
      - "5432:5432"
    volumes:
      - papermind_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U papermind -d papermind"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Test database. tests/conftest.py binds to 5433/papermind_test.
  db-test:
    image: pgvector/pgvector:pg16
    container_name: papermind-db-test
    environment:
      POSTGRES_USER: papermind
      POSTGRES_PASSWORD: papermind
      POSTGRES_DB: papermind_test
    ports:
      - "5433:5432"
    volumes:
      - papermind_pgdata_test:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U papermind -d papermind_test"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  papermind_pgdata:
  papermind_pgdata_test: